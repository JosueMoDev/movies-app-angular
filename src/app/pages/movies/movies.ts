import { Component } from "@angular/core";
import { Layout } from "@app/pages/components/layout/layout";

@Component({
  selector: "movies",
  imports: [Layout],
  templateUrl: "./movies.html",
})
export default class MoviesList {}
